import { styled } from "@/styled-system/jsx";

export const MutedText = styled("label", {
  base: {
    display: "block",
    fontSize: "base",
    fontWeight: "semibold",
    color: "input.label",
    mb: "1",
  },
});
